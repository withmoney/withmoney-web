import React from 'react';
import Container from '../Components/Container';
import { Buttons } from '../Components/Buttons';

const Credit = () => {
  return (
    <Container>
      <Buttons activeButton="Credit" />
    </Container>
  );
};

export default Credit;
