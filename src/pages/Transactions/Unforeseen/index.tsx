import React from 'react';
import Container from '../Components/Container';
import { Buttons } from '../Components/Buttons';

const Unforeseen = () => {
  return (
    <Container>
      <Buttons activeButton="Unforeseen" />
    </Container>
  );
};

export default Unforeseen;
