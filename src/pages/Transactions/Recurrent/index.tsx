import React from 'react';
import Container from '../Components/Container';
import { Buttons } from '../Components/Buttons';

const Recurrent = () => {
  return (
    <Container>
      <Buttons activeButton="Recurrent" />
    </Container>
  );
};

export default Recurrent;
